# Inventory host dev - tambahkan IP server dev di sini secara MANUAL.
# Konsisten dengan mau-apps' single-node-per-environment convention: host IP
# tidak di-auto-append oleh provision-server.sh/init-server.sh.
#
# Contoh (hapus '#' dan ganti IP + user admin awal sesuai provider):
# 203.0.113.10 ansible_user=root

[dev]
