# Inventory host prod - tambahkan IP server prod di sini secara MANUAL.
# Host IP tidak di-auto-append oleh provision-server.sh/init-server.sh.
#
# TODO: enem-landing sudah punya VPS prod (dipakai .drone.yml lama lewat
# secret SSH_HOST), tapi nilai secret itu tidak bisa dibaca dari sini -
# isi manual begitu tersedia (`gh secret list`/dashboard Drone/provider VPS),
# JANGAN commit IP asli tanpa mengecek ulang - lihat catatan story 09
# (issues/09-dev-tooling-scripts.md).
#
# Contoh (hapus '#' dan ganti IP + user admin awal sesuai provider):
# 203.0.113.20 ansible_user=root

[prod]
