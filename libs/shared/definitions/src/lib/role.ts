/**
 * Mirrors mau-apps' `Role` enum (`libs/shared/definitions/src/lib/role.enum.ts`)
 * 1:1 — same three values, same string constants.
 */
export enum Role {
  User = 'USER',
  Admin = 'ADMIN',
  SuperAdmin = 'SUPER_ADMIN',
}
